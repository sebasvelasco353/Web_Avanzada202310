interface IProps {
    name : string,
    age : number,
    career : string,
    semester : number,
}

export const Table = (props : IProps) => {
    return (
        <section className={"student"}>

        </section>
    );
}